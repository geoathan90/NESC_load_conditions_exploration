# Tests

The focused suite covers unit conversions, precipitation-category masks, local and
remote-style discovery, longitude normalization, strict step-type validation/alignment,
and event continuity across intentionally absent months.

Run with `pytest -q` after installing `requirements.txt`.
