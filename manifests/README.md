# Manifests

`larisa_drive_inventory.csv` is generated by the Drive synchronizer and records
the remote ID, stable relative path, local availability and byte size for every
discovered source file. Per-file NetCDF content validation is written separately
to each period's `validation_manifest.csv` under `outputs/`.
